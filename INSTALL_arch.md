Install these:
```bash
sudo pacman -S wofi wezterm waybar ttf-agave-nerd
yay swayfx
paru swayfx
```

And optionally these:
```bash
paru bongocat
yay bongocat
sudo pacman -S neovim rust
```

This commands were suggested by [@IsThisALis](https://github.com/IsThisALis). I ([@quanttum1](https://github.com/quanttum1)) don't use Arch so I can't comfirm them, but I know @IsThisALis personally so I trust them
