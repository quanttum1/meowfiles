Install these:
```bash
sudo pacman -S wofi wezterm waybar ttf-agave-nerd
yay swayfx
paru swayfx
yay bongocat
```

And optionally these:
```bash
paru bongocat
sudo pacman -S neovim rust
```

This commands were suggested by @IsThisALis. I (@quanttum1) don't use Arch so I can't comfirm them, but I know @IsThisALis personally so I trust them
